<%= form_with(model: @patients, url:"user_patients_path", method:"post") do |form| %>
    <div>
      <%= form.label :patientName %><br>
      <%= form.text_field :patientName %>
    </div>
    <div>
      <%= form.label :treatment_id %><br>
      <select name="patient[treatment_id]" id="treatment_id">
        <% @categorys.each do |category| %>
          
            <optgroup label=<%= category.categoryName %> >
              
            <% category.treatments.each do |treatment| %>
              <option value=<%= treatment.id %> >
                <%= treatment.treatmentName %>
              </option>
            <%end %>
            </optgroup>
          
        <% end %>
      </select>
    </div>
    <div>
      <%= form.submit %>
    </div>
  
    
  <% end %>
